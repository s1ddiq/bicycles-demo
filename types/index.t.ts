interface Button {
    text: string;
    type: string;
    className?: string;
}

// rename to bike card
interface ItemCard {
    name: string;
    price: number;
    image: string;
}

interface StatCard {
    percentage: string;
    text: string;
}